# Copilot Bridge v1.3.1 原生体验与全屏连接保护设计

> 日期：2026-07-24（Asia/Shanghai）
> 状态：Phase 29–33 已通过；v1.3.1 已正式发布
> 前置版本：v1.3.0 已发布
> 上位设计：[PROJECT-DESIGN.md](./PROJECT-DESIGN.md)
> 执行路线图：[EXECUTION-ROADMAP.md](./EXECUTION-ROADMAP.md)

## 1. 版本结论

v1.3.1 是一个窄范围桌面体验与兼容性版本：先用原生 WPF 为已有状态变化增加克制的微动效，再针对团队在全屏游戏期间被 Edge 远程访问授权提示切出的真实问题，在建立新 CDP 会话前增加默认开启的全屏保护。它不改变会话存储、统计、DOM 操作或发送语义。

本范围来自路线图中已经记录、但未进入 v1.2.2 或 v1.3.0 的“原生 WPF 微动效”候选。当前源码也证明按钮和开关仍以瞬时 Setter 切换，通知直接显示/隐藏，而拖拽已有 90–190 ms 动效但尚未统一处理系统动画关闭状态。

Amicro（React + Motion）只保留为交互节奏参考；本版本不复制其代码，不引入 WebView、React、Motion 或新 NuGet 依赖，因此发布物不新增其运行时代码或许可文件。

## 2. 必须保持的边界

- 继续保持一个生产项目、一个测试项目和一个生产可执行文件。
- 四个 MCP 工具、项目权限、咨询预算和本地统计语义保持不变。
- 不修改 Markdown、sidecar、迁移或托盘生命周期；设置只增加一个默认开启的布尔值。
- 不修改模型选择、页面绑定、单写入锁、发送状态和禁止自动重发语义。
- 不增加第三方 UI 包、动画框架、页面路由、MVVM 重构或通用行为系统。
- 不使用 Computer Use、OCR、UIA 或物理输入作为产品运行时能力。
- 不新增后台服务、共享 CDP broker、跨进程 RPC、进程名白名单或第二套浏览器自动化。

## 3. 唯一交付范围

### 3.1 统一动画策略

- 使用 `SystemParameters.ClientAreaAnimation` 作为唯一系统动画开关。
- 系统动画开启时执行有界的一次性动画；关闭时直接呈现最终状态，不延迟业务动作。
- 时长固定在 90–190 ms，使用 WPF 原生 `DoubleAnimation`、`Opacity`、`ScaleTransform`、`TranslateTransform` 和二次缓动。
- 动画不能改变布局尺寸、命中区域、键盘焦点、自动化名称或业务状态。

### 3.2 三类新增微动效

| 场景 | 动画 | 时长 | 关闭动画时 |
|---|---|---:|---|
| 主要、次要、导航与快捷方式按钮 | hover 透明度过渡；pressed 缩放到 0.98 后恢复 | 90–140 ms | 保留现有即时 hover/pressed 状态 |
| 通知与操作结果 | 首次显示时从 `Y=-6`、`Opacity=0` 淡入最终位置 | 180 ms | 立即显示最终状态 |
| 设置与托盘开关 | thumb 在两个端点间平移，轨道颜色仍由真实选中状态决定 | 160 ms | thumb 立即到达最终端点 |

现有项目、模型和会话拖拽继续使用当前 90–190 ms 反馈，但必须经过同一个系统动画开关；关闭动画时直接设置最终透明度和 transform。

### 3.3 明确排除的效果

- magnetic 鼠标追踪、无限 glare/pulse、shake、focus blur、3D 卡片和持续循环动画。
- 页面切换转场、窗口启动动画、托盘图标动画和列表虚拟化重写。
- 把动画作为成功、错误、选中、开关状态或拖拽目标的唯一信号。

## 4. 阶段与门禁

### Phase 29：范围与开发基线冻结

- 新建设计文档并更新总设计、路线图和 README。
- 正式发布时源码、Plugin、发布脚本与下载说明统一为 `1.3.1`。
- 通过标准 Debug 构建、172 项既有测试、项目/依赖预算和 `.workbuddy/` 排除检查。
- 本地提交：`phase 29: freeze v1.3.1 native motion scope`。

### Phase 30：原生微动效纵切

- 完成统一动画策略、三类新增微动效和现有拖拽的减少动画分支。
- 自动化测试验证时长上限、允许属性、系统动画关闭分支和业务边界未改变。
- 运行 Debug/Release 构建和完整测试；不发送 Copilot 消息，不修改用户工作区。
- 本地提交：`phase 30: add reduced-motion-aware microinteractions`。

### Phase 31：全屏保护范围与基线冻结

- 记录团队在 v1.2.2 全屏游戏期间被切出的真实案例，并把保护点严格限定在新 CDP 连接之前。
- 固定默认开启的设置、只读前台全屏检测、稳定错误码、显式 GUI 手动连接例外和诊断日志。
- 已复用且仍有效的 CDP 会话不执行全屏检测；保护触发时必须在连接和发送前停止。
- Debug/Release、176 项既有测试、项目/依赖/MCP 工具预算和 `.workbuddy/` 排除通过。
- 本地提交：`phase 31: freeze fullscreen connection protection`。

### Phase 32：全屏连接保护纵切

- 使用 `GetForegroundWindow`、`GetWindowRect`、`MonitorFromWindow` 和 `GetMonitorInfo` 只读判断前台窗口是否覆盖所在显示器。
- MCP 或 GUI 自动刷新需要建立新 CDP 会话且检测到全屏时，返回 `fullscreen_guard_active`，不调用 `ConnectOverCDPAsync`。
- GUI 的“刷新状态”“绑定”和用户主动咨询属于显式动作，可以建立连接；设置页允许用户关闭保护。
- 现有有效 CDP 会话继续复用，不因全屏状态中断；日志只记录保护结果和窗口/显示器几何，不记录标题或正文。
- 自动化测试覆盖默认值、设置持久化、几何边界、禁用/显式例外、稳定错误码和零连接调用。
- 本地提交：`phase 32: guard new CDP connections during fullscreen use`。

### Phase 33：视觉与发布候选门禁

- 启动一个新的 Bridge 候选实例，交由用户观察浅色、深色、动画开/关、全屏保护设置、键盘路径和最小窗口。
- Windows 动画开启与关闭状态分别验收；不能用代码存在代替实机结论。
- 在非游戏状态完成一次显式连接授权，再验证已授权会话在全屏期间可复用，以及冷 MCP 连接被保护拦截。
- Debug/Release、完整测试、复杂度预算、包清单和隔离升级/回退通过后才能形成候选。
- 打包、安装、标签、推送和 GitHub Release 仍需用户另行明确授权。
- 本地提交：`phase 33: harden v1.3.1 release candidate`。

## 5. 复杂度预算与停止条件

- v1.3.0 记录口径为 7476 行 C#；计入两个 WPF XAML 后的基线为 7948 行。加入全屏保护后，v1.3.1 的 C# + XAML 生产增量上限修订为 420 行。
- 直接生产 NuGet 依赖保持 2 个；MCP 工具保持 4 个。
- 除现有动画策略文件外，最多增加一个小型 Windows 全屏检测文件；不建立 attached behavior、动画 DSL、窗口管理框架或第二套主题资源。
- 如果按钮动画要求替换现有控件体系、复制模板或破坏键盘/高对比度状态，停止在 Phase 30 收敛实现。
- 如果 Windows 动画关闭后仍出现过渡、延迟或状态不一致，Phase 30 门禁失败，不进入候选阶段。
- 如果保护无法在调用 `ConnectOverCDPAsync` 前作出决定，或必须使用窗口标题、进程白名单、Hook、服务或跨进程 broker 才能工作，Phase 32 失败。

## 6. Phase 30 实现结果

- 新增单一 `MainWindow.Motion.cs`，集中处理按钮、通知、开关和拖拽的原生 WPF 动画；没有引入 attached behavior、动画 DSL 或新依赖。
- 按钮 hover、鼠标/键盘 pressed、通知首次显示和开关 thumb 已接入；通知重复更新不重复播放入场动画。
- 现有项目、模型和会话拖拽复用同一动画执行器；`SystemParameters.ClientAreaAnimation=false` 时直接设置最终状态。
- 开关只动画 `TranslateTransform.X`，按钮只动画 `Opacity` 与 `ScaleTransform`，通知与拖拽只动画 `Opacity`/`TranslateTransform`，不改变布局属性。
- Debug/Release 均为 0 警告、0 错误，176/176 测试通过；新增测试覆盖系统开关、90–190 ms 时长、开关非布局平移和允许的渲染属性。
- 当前生产基线为 8156 行 C# + XAML，相对 v1.3.0 增加 208 行，低于 260 行上限；仍为 2 个项目、2 个直接生产依赖、4 个 MCP 工具，`.workbuddy/` 跟踪文件为 0。
- 本阶段未启动 GUI 视觉验收、未发送 Copilot 消息、未修改用户工作区，也未打包、安装、标记、推送或发布。

## 7. Phase 31 范围冻结结果

- 静态审计确认生产代码没有 `BringToFront`、`SetForegroundWindow`、`SendInput`、UI Automation 或物理输入路径；托盘恢复是唯一显式 `Activate()` / `Focus()` 路径。
- `ConnectOverCDPAsync` 会话只在当前 GUI 或 MCP 进程内复用。新 MCP 进程、Edge 重启、会话丢失或重新授权可能触发 Edge Remote access 提示，这是保护要拦截的唯一边界。
- 全屏保护默认开启，但只阻止自动或 MCP 冷连接；它不终止现有会话、不扫描进程名、不隐藏或操作游戏窗口。
- 保护触发属于明确的提交前失败：发送零条消息，`canRetrySafely=true`，并按现有规则返回 `new_consultation` 或 `reuse_consultation`。
- Debug/Release 均为 0 警告、0 错误，176/176 测试通过；基线仍为 8156 行 C# + XAML、2 个项目、2 个直接生产依赖、4 个 MCP 工具，`.workbuddy/` 跟踪文件为 0。
- 未连接 Edge、未发送 Copilot 消息、未修改用户工作区，未打包、安装、标记、推送或发布。

## 8. Phase 32 实现结果

- 新增单一 `FullscreenConnectionGuard.cs`，只读调用 `GetForegroundWindow`、`GetWindowRect`、`MonitorFromWindow` 和 `GetMonitorInfo`；按当前前台窗口与所在显示器的边缘覆盖关系判断，不读取标题或进程名。
- `EdgeSessionAdapter.ConnectAsync` 在端点解析、Playwright 创建和 `ConnectOverCDPAsync` 之前执行保护。GUI 自动刷新和 MCP 冷连接不绕过；显式 GUI 刷新、绑定、导入和用户咨询可以建立连接。
- 设置新增默认开启的 `fullscreenProtectionEnabled`，设置页可关闭；`copilot_bridge_status` 只读返回该值，MCP 工具数量和写工具输入 schema 不变。
- 全屏拦截映射为提交前 `fullscreen_guard_active`：发送零条消息、`canRetrySafely=true`，新咨询返回 `new_consultation`，已有咨询返回 `reuse_consultation`。
- 日志只记录 `fullscreen_connection_blocked` 及窗口/显示器矩形，不记录标题、进程名、prompt 或回复正文。
- 新增测试覆盖默认与持久化、主屏/负坐标副屏几何、8 px 边界、任务栏间隙、禁用与显式例外、端点解析前拦截、安全重试契约和 GUI/MCP 调用边界。
- Debug/Release 均为 0 警告、0 错误，186/186 测试通过；8302 行 C# + XAML，相对 v1.3.0 增加 354 行，低于 420 行上限；仍为 2 个项目、2 个直接生产依赖、4 个 MCP 工具，`.workbuddy/` 跟踪文件为 0。
- 未连接 Edge、未发送 Copilot 消息、未修改用户工作区，也未打包、安装、标记、推送或发布。
