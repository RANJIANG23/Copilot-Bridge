# Copilot Bridge v1.3.2 多 Agent 中性化设计

> 日期：2026-07-26（Asia/Shanghai）
> 状态：Phase 34–36 已通过；v1.3.2 已正式发布
> 前置版本：v1.3.1 已正式发布
> 上位设计：[PROJECT-DESIGN.md](./PROJECT-DESIGN.md)
> 执行路线图：[EXECUTION-ROADMAP.md](./EXECUTION-ROADMAP.md)

## 1. 版本结论

v1.3.2 将当前已经基于标准 STDIO MCP 的 Copilot Bridge，从“只能由 Codex 理解的产品表达”收敛为“由任意符合约束的调用 Agent 使用的本地桥接工具”。它保留 Codex Plugin 作为已验证的一种宿主集成，但 GUI、MCP instructions、自动征询触发词和通用安装说明不再把 Codex 当成唯一调用方。

这个版本实现的是 **调用方中性化**，不是多 Agent 运行时框架：

- Bridge 仍只有一个 STDIO MCP server、一个咨询写锁和一个专用 Copilot 标签页。
- 不识别、注册或调度不同 Agent，不保存调用方账号、密钥或会话。
- 不增加 WorkBuddy、OpenClaw、opencode 或其他产品的专用适配器。
- 不增加 Provider 抽象、后台服务、端口、队列、RPC 或第二套浏览器自动化。

## 2. Phase 34 实施前已核验的耦合

- 会话本地角色已经使用 `agent`，项目权限也已经显示为“Agent 访问权限”。
- MCP 使用标准 STDIO，四个工具本身不依赖 Codex SDK。
- GUI 征询策略仍显示“Codex 可自动征询”。
- `consult_copilot.trigger` 只接受 `user_explicit`、`codex_auto` 和 `required_checkpoint`。
- MCP server instructions 仍写明由 Codex 核验和执行。
- README 与安装流程只描述 Codex Plugin，未给其他 STDIO MCP 客户端一个受支持的最小接入契约。

## 3. Phase 35 交付范围

### 3.1 GUI 与本地文案

- 将面向用户的“Codex 可自动征询”改为“Agent 可自动征询”。
- 将复制、权限和说明中的调用方表述统一为“Agent”或“调用 Agent”。
- 保留明确属于 Codex Plugin、Codex 安装命令和 Codex Skill 的专有名称，不把具体宿主名称错误泛化。
- 不改写用户项目名、Markdown、Copilot 标题或历史会话正文。

### 3.2 MCP 调用契约

- `consult_copilot.trigger` 新增 `agent_auto`，其策略语义与旧 `codex_auto` 完全相同。
- 继续接受 `codex_auto`，避免已安装 Codex Plugin 和旧客户端失效。
- `user_explicit` 与 `required_checkpoint` 保持不变。
- MCP server instructions 使用“calling agent”，继续要求调用方核验 Copilot 建议并负责实际执行。
- `copilot_bridge_status.consultationPolicy` 在 v1.3.2 继续保留已有 `codex_may_consult` 值，避免补丁版本破坏现有读取方；GUI 和新文档将其标明为兼容旧值。
- 四个工具的名称、数量、安全注解和其他输入/输出字段保持不变。

### 3.3 通用 STDIO MCP 接入说明

- 新增一份调用方中立的本地 MCP 接入说明，提供可执行文件、`--mcp` 参数、工作目录、四工具安全语义和 `agent_auto` 使用规则。
- 通用说明不声称已经验证任何未测试的第三方 Agent 产品，也不猜测其配置文件格式。
- 现有 Codex Plugin、安装脚本和逐工具审批继续保留，作为已验证的具体集成。
- App-only 安装继续复用现有 `-SkipCodexPlugin` 路径，不创建第二套安装器。

## 4. 明确不做

- 不实现多个 Agent 并发咨询、路由、队列、身份登记或共享 CDP broker。
- 不自动把本地项目、工作区或历史会话发送给任何 Agent 或 Copilot。
- 不增加 Agent 名称、来源或客户端 ID 到咨询写工具。
- 不把 `mode`、`model` 或项目内容加入 `consult_copilot`。
- 不实现 WorkBuddy、OpenClaw、opencode、Claude Code 或其他客户端专用安装。
- 不实现 Adaptive 自动路由、关键词模式推断、自动升级协作模式或 Agent 投票。
- 不修改 Edge/CDP/DOM、模型选择、存储格式、预算、全屏保护或禁止不确定重发语义。
- 不打包、安装、标记、推送或发布 v1.3.2，除非用户另行明确授权。

## 5. Phase 与门禁

### Phase 34：范围与开发基线冻结

- 新建本设计并同步总体设计、路线图和 README。
- 源码进入 `1.3.2-dev`；Plugin、发布脚本、下载与安装说明继续保持已发布的 `1.3.1`。
- 基线保持 8302 行 C# + XAML、2 个项目、2 个直接生产依赖、4 个 MCP 工具和 0 个跟踪的 `.workbuddy/` 文件。
- Debug 构建和 186 项既有测试通过后创建本地提交：`phase 34: freeze v1.3.2 agent-neutral scope`。

### Phase 35：多 Agent 中性化纵切

- 完成 GUI、UiText、README/MCP 文档和 server instructions 的调用方中性化。
- 新增 `agent_auto`，并以测试证明它与 `codex_auto` 使用同一策略门禁。
- 验证旧设置、旧 trigger、Codex Plugin 和四工具 schema 不回归。
- 不连接 Edge、不发送 Copilot 消息、不修改用户工作区。
- 本地提交：`phase 35: neutralize calling-agent integration`。

进度记录（2026-07-26）：

- GUI 征询策略和复制提示已改为 Agent/调用 Agent 语义。
- `agent_auto` 与旧 `codex_auto` 共用同一策略门禁；未知 trigger 继续返回 `invalid_trigger`。
- MCP instructions、工具 schema 描述、README、安装说明和通用 STDIO MCP 接入文档已中性化。
- 直接 STDIO MCP 协议测试确认四工具数量与安全注解不变，instructions 同时说明 calling agent、`agent_auto` 和旧 alias。
- Debug 构建 0 警告/0 错误，190/190 测试通过；生产 C# + XAML 仍为 8302 行。

### Phase 36：兼容与发布候选门禁

- 使用直接 STDIO MCP 客户端完成 initialize、tools/list、status 和只读查询；不以 Codex Plugin 作为唯一验收路径。
- 新启动 GUI，由用户确认中英文文案、征询策略、权限说明和最小窗口没有回归。
- 完成 Debug/Release、完整测试、复杂度预算、包清单和 1.3.1 → 1.3.2 隔离升级/回退。
- 用户已于 2026-07-28 明确授权并完成正式安装、标签、推送和 GitHub Release。
- 本地提交：`phase 36: harden v1.3.2 agent-neutral candidate`。

自动化进度（2026-07-26）：

- Debug/Release 均为 0 警告、0 错误，两个配置各 192/192 测试通过。
- 候选 ZIP 的 611 个清单文件全部匹配，619 个归档条目包含单一生产 EXE、通用 `MCP-CLIENTS.md`，不包含 `.workbuddy/`；程序与 Plugin 均为 1.3.2。
- 隔离环境完成 1.3.1→1.3.2、四工具 generic STDIO、状态与只读检索、卸载和 1.3.1 回退；用户数据与正式宿主配置保持不变。
- `-SkipCodexPlugin` app-only 路径另行完成安装、指南留存、相同 STDIO 门禁和完整卸载。
- 四个工具名称、Plugin 1.3.2、旧状态值 `codex_may_consult`、状态与检索响应字段均作为严格通过条件；Plugin 与 app-only STDIO transcript 分开保存。证据位于 `artifacts/upgrade-test/v1.3.2-20260726225007`。用户已确认可发布，作为中英文 GUI 目视门禁的最终验收授权。

### Phase 36 完成性审计

| 明确要求 | 当前结论 | 权威证据 |
|---|---|---|
| 通用 STDIO initialize、tools/list、status、只读查询 | 已证明 | Plugin 与 app-only 两条独立 transcript；四工具名称、状态和检索字段均严格匹配 |
| `agent_auto` 与旧 `codex_auto` 兼容 | 已证明 | Coordinator 策略测试、MCP schema 测试和候选 instructions |
| 旧 `codex_may_consult` 状态值不变 | 已证明 | 快速协议回归及两条打包 EXE 隔离路径均返回该值 |
| Debug/Release、完整测试与复杂度预算 | 已证明 | 两个配置各 192/192；8302 行 C# + XAML、2 个项目、2 个依赖、4 个工具 |
| 候选包清单、单一 EXE、通用指南、`.workbuddy/` 排除 | 已证明 | 611/611 清单匹配、619 个归档条目、单一生产 EXE、通用指南存在、跟踪与归档均为 0 |
| 1.3.1→1.3.2 升级、卸载和 1.3.1 回退 | 已证明 | 隔离结果 `passed`，用户数据、正式宿主配置保持不变 |
| app-only 安装、指南留存、STDIO 和完整卸载 | 已证明 | `-SkipCodexPlugin` 隔离路径全部为 true |
| 中英文文案、征询策略、权限说明、最小窗口实机显示 | 已确认 | 用户于 2026-07-28 确认 v1.3.2 可以发布；静态翻译、最小尺寸和滚动结构测试已通过 |
| 正式安装、标签、推送和 GitHub Release | 已完成 | PR #9 合并提交 `6f14ae9` 已标记为 `v1.3.2`；公开 ZIP SHA-256 为 `ed8ccbba14ba965ba805b55dca79e411e008697e8a21c70fad7d2304b0f5fe79`，本机正式安装与 Plugin 均已升级并核验 |

## 6. 预算与停止条件

- Phase 34 基线为 8302 行 C# + XAML；v1.3.2 生产增量上限为 220 行。
- 继续保持一个生产项目、一个测试项目、一个 EXE、2 个直接生产依赖和 4 个 MCP 工具。
- 如果中性化要求客户端身份协议、额外进程、端口、队列、Provider 框架或特定第三方 SDK，停止 Phase 35 并重新确认产品范围。
- 如果 `agent_auto` 破坏旧 `codex_auto`、策略门禁、发送状态或安全注解，停止在 Phase 35 修复，不进入候选阶段。
- 如果通用接入无法仅依靠标准 STDIO MCP 描述清楚，不用猜测第三方配置格式伪造“已支持”。
