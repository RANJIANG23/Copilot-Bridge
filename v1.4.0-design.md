# Copilot Bridge v1.4.0 设计：工作台与桌面就绪

> 日期：2026-07-27（Asia/Shanghai）
> 状态：v1.4.0 已正式发布；Phase 37–39 已通过；Phase 40 自动门禁与正式发布完成、人工视觉门禁未完成，保持部分完成
> 上位设计：[PROJECT-DESIGN.md](./PROJECT-DESIGN.md)
> 执行路线：[EXECUTION-ROADMAP.md](./EXECUTION-ROADMAP.md)

## 1. 版本目标

v1.4.0 对 GLM 5.2 历史建议做事实校正后，只实施仍有用户价值、能够复用现有边界且不引入第二套架构的部分：

1. 把本地会话从“可检索文件”补成可整理、可复用、可恢复的轻量工作台；
2. 补齐 Windows 桌面应用仍缺少的首次使用、可选开机启动、高对比度和有限响应式能力；
3. 保持四工具 MCP、Edge CDP/DOM、单写入锁、预算快照、项目权限和禁止不确定重发的现有契约。

本版不以纯视觉优化冒充重大能力升级，也不把 GLM 草案当成权威设计。每项建议必须先与当前源码和已经发布的行为核对。

## 2. GLM 5.2 建议事实对比

| 建议 | 当前事实 | v1.4.0 决策 |
|---|---|---|
| 浅色/深色 Copilot 风格、卡片、Fluent 图标、中文字体 | v1.1.2 已完成 | 已实施，不重复 |
| 单一主题资源与按钮/输入/卡片状态 | v1.2.1 已完成受控 `ResourceDictionary`、hover/pressed/focus/disabled | 已实施，不为形式拆分资源字典 |
| 键盘排序、辅助技术名称、约 40px 触控目标 | v1.2.1 已完成 | 已实施，不重复 |
| 静默刷新、真空状态的基础表现 | v1.1.2 已完成静默刷新，部分页面已有空状态 | 保留；本版只补工作台恢复状态 |
| 会话正文/元数据分离 | v1.2.2 存储 v2 已完成 | 已实施，不改变文件事务协议 |
| 系统托盘 | v1.2.2 已完成且默认关闭 | 已实施，不重复 |
| 减少动画 | v1.3.1 已遵从 `SystemParameters.ClientAreaAnimation` | 已实施，不增加动画框架 |
| 多 Agent 调用方中性化 | v1.3.2 已正式发布，Phase 36 已通过 | 已实施；v1.4.0 继续保持四工具与调用方中立契约 |
| Design Token 全量提取 | 已有主题画刷和常用尺寸，但 XAML 仍有局部常量 | 仅为本版新增控件复用现有 token；不做无价值的全文件机械改写 |
| Onboarding / 首次使用 | 未实施 | 实施可跳过、可重开的本地清单；不自动操作 Edge 或 Copilot |
| Windows 高对比度 | 未实施 | 实施系统高对比度映射；不新增第三主题设置值 |
| 完整响应式布局 | 目前只有最小窗口和历史列宽阈值 | 实施有限响应式侧栏/内容密度；不改写为新 UI 框架 |
| 开机启动 | 未实施；现有快捷方式管理已可复用 | 实施默认关闭、当前用户 Startup 文件夹快捷方式；只启动 GUI |
| 会话标签与提示模板 | 未实施 | 实施本地、轻量、可搜索的标签与用户模板 |
| 显式失败恢复 | 已有复制 Markdown 和稳定错误，但缺少统一“打开原对话”入口 | 补充打开原 Copilot URL、复制待发送文本和可恢复提示 |
| 自动标题双向同步 | 已保存 Copilot 初始/当前/历史标题，但自动同步会引入后台写入 | 不实施自动同步；继续由用户显式改名或导入 |
| 人可读文件名迁移 | v2 使用稳定 `conversation-{id}.md`，外部引用和迁移成本未知 | 延期；不为外观破坏稳定路径 |
| Adaptive 自动模式/关键词路由 | 没有可信分类器，可能产生隐式额外发送 | 拒绝；模式仍只由用户在 GUI 控制 |
| 每对话模型控制 | 模型策略是 GUI 全局用户控制 | 拒绝；不允许调用 Agent 暗改模型 |
| 多 profile、Provider、专用第三方适配器 | 会扩大身份、配置和自动化边界 | 拒绝；继续使用通用 STDIO MCP |
| HTML/PDF 导出 | Markdown 已是权威、可移植正文 | 延期；本版不引入渲染/打印栈 |
| 全局热键直接发送 | 会绕过可见上下文、模式和提交确认 | 拒绝 |
| WorkBuddy 运行时接入 | `.workbuddy/` 只能作为开发草稿且不得入包 | 拒绝；跟踪和发布输入继续为零 |

## 3. Phase 37：范围与开发基线

### 3.1 交付

- 新增本文件并把 GLM 建议分类为“已实施 / 本版实施 / 延期或拒绝”。
- 源码版本进入 `1.4.0-dev`；已发布的 1.3.2 构建、Plugin、安装和隔离升级证据保持不变。
- 合并并保留 v1.3.2 Phase 36 的正式发布记录，不回退已经完成的标签、Release 与本机安装事实。

### 3.2 门禁

- Debug 构建 0 警告、0 错误，既有 192 项测试全部通过。
- 恰好一个生产项目、一个测试项目、一个生产 EXE、两个直接生产依赖、四个 MCP 工具。
- `.workbuddy/` Git 跟踪文件为 0。
- 本地提交：`phase 37: freeze v1.4.0 workbench scope`。

## 4. Phase 38：本地工作台纵切

### 4.1 会话标签

- `ConversationDocument` 增加顺序稳定、去空白、大小写不重复的标签集合。
- 存储 v2 sidecar 保存标签；旧 sidecar、旧 Markdown 和只读 MCP 保持兼容。
- 对话管理允许为所选会话编辑标签，搜索覆盖标签但不扩大项目授权范围。
- 标签不写进正文、不改变会话 ID、不改名现有文件。

### 4.2 提示模板

- 用户可以保存、套用、改名和删除轻量提示模板。
- 模板只填入现有咨询输入框，绝不自动发送，也不创建第二条写入路径。
- 模板使用现有设置目录中的独立 JSON 文档，采用临时文件替换和进程内单写入边界。
- 首版不支持变量语言、远程同步、团队共享或模板市场。

### 4.3 恢复动作

- 所选会话有可信 Copilot URL 时可由用户显式打开原对话。
- 当前咨询失败或被门禁拦截时，用户可以复制仍在输入框中的待发送文本。
- URL 只允许现有精确 Copilot host；未知或缺失 URL 不启动外部进程。
- 不自动关闭网页浮层、不自动重试、不自动重发。

### 4.4 预算与门禁

- Phase 38 生产 C# + XAML 增量上限 900 行。
- 不增加生产 NuGet 依赖、MCP 工具、服务、端口、数据库或浏览器栈。
- 新增单元测试覆盖标签规范化/兼容存储/检索、模板持久化和 URL 白名单。
- 本地提交：`phase 38: add reusable local workbench tools`。

## 5. Phase 39：Windows 桌面就绪

### 5.1 可选开机启动

- 设置页增加默认关闭的“登录 Windows 后启动 Bridge”。
- 复用 `ShortcutManager`，只在当前用户 Startup 文件夹创建或删除指向已安装/当前 EXE 的快捷方式。
- 开机只启动 GUI；是否保持 MCP 后台进程继续由既有独立设置控制。
- 设置写入失败时回滚 UI 值并显示可恢复错误，不伪报成功。

### 5.2 首次使用

- 首次运行显示可跳过的本地清单：工作区、Edge 远程调试、Copilot 标签页、协作模式和调用方接入。
- 清单只解释和导航到既有页面，不自动登录、不自动发消息、不自动改 Edge 配置。
- 完成或跳过状态持久化；设置页可重新打开。

### 5.3 高对比度与有限响应式

- Windows 高对比度开启时使用系统窗口、文本、边框、高亮和高亮文本颜色。
- 关闭高对比度后恢复用户选择的浅色/深色主题。
- 在现有最小窗口内按阈值收紧侧栏、隐藏次要说明并调整工作台列宽；不引入 hamburger 导航或新窗口体系。
- 保持键盘焦点、AutomationProperties、约 40px 操作目标和减少动画行为。

### 5.4 预算与门禁

- Phase 39 生产 C# + XAML 增量上限 700 行；v1.4.0 总增量不超过 1600 行。
- 自动化测试覆盖启动快捷方式路径/幂等、首次使用状态、高对比度切换和响应式阈值。
- 用户不可接管或安全桌面阻止的纯视觉检查按用户指示跳过，状态保持“部分完成”，其余门禁继续。
- 本地提交：`phase 39: complete Windows desktop readiness`。

## 6. Phase 40：候选加固

- Debug/Release 构建与完整测试全部通过。
- 核对生产增量、项目/依赖/EXE/MCP 工具预算和 `.workbuddy/` 排除。
- 打包 1.4.0 候选并验证清单、单一 EXE、Plugin 版本和通用 MCP 文档。
- 在隔离目录验证 1.3.1 或最新已发布版到 1.4.0 的升级、卸载和回退；不得改动当前正式安装。
- GUI 人工门禁无法安全自动化时记录为未完成，不影响继续执行其他自动门禁，但 Phase 40 不伪报“通过”。
- 用户已于 2026-07-30 明确授权并完成标签、推送、GitHub Release、正式安装和开始菜单快捷方式核验。
- 本地提交：`phase 40: harden v1.4.0 candidate`（仅在所有规定门禁通过时）。

## 7. 不变契约

- 生产交互只使用 Edge CDP 与专用 Copilot 标签页 DOM。
- 提交动作变得不确定后绝不自动重发。
- 协作模式和模型策略只能由用户在 GUI 中控制。
- 四个 MCP 工具及其安全注解保持不变。
- 项目权限默认关闭；只读查询不连接 Edge、不迁移、不改写工作区。
- 一个生产项目、一个测试项目、一个生产 EXE、最多五个直接生产依赖。
- 不增加数据库、本地 Web server、daemon、队列、自定义 RPC、Provider 框架或第二浏览器栈。
- `.workbuddy/` 永不跟踪、提交、复制、打包或发布。
